# GitHub 发布指引（给作者）

本地已完成：代码整理成干净 git 仓库（26 个源文件，main 分支，首次提交完成），
发布 zip 已生成：`合成大西瓜自动化_发布_v1.0.0.zip`（87MB，解压即用，不含任何账号数据）。

## 一、创建 GitHub 仓库并推送源码

1. 浏览器登录 GitHub → New repository → 名称建议 `watermelon-bot`（Public）→ Create（**不要**勾选 README/.gitignore，本地已有）。
2. 命令行推送（项目目录 `C:\Users\Administrator\Desktop\大西瓜`）：

```bash
git remote add origin https://github.com/<你的用户名>/watermelon-bot.git
git branch -M main
git push -u origin main
```

首次推送会要求 GitHub 用户名 + Personal Access Token（不是密码）：
- 生成 Token：GitHub → Settings → Developer settings → Personal access tokens → Tokens(classic) → Generate new token
- 勾选 `repo` 权限 → 生成后复制（只显示一次）
- push 时密码栏粘贴该 Token 即可

## 二、上传 Release（给用户下载 exe）

1. GitHub 仓库页 → Releases → Create a new release
2. Tag：`v1.0.0`，标题：`v1.0.0 桌面版`
3. 拖入 `合成大西瓜自动化_发布_v1.0.0.zip`（87MB，GitHub 单文件上限 2GB，直接传）
4. 说明文案：复制 README.md 的「功能/快速开始」段落

## 三、部署数据收数服务（腾讯云函数，0 成本）

按 `server_cloud/README.md`：
1. 开通 COS 建私有桶 → 2. 部署 SCF 云函数（上传 server_cloud/index.py，配 5 个环境变量）
3. 建 API 网关 POST 触发器 → 拿到 URL
4. **关键一步**：把 URL + AUTH_TOKEN 填回客户端源码 `telemetry.py` 顶部的
   `ENDPOINT` / `AUTH_TOKEN`（或打包时设环境变量 WM_TELEMETRY_URL / WM_AUTH_TOKEN），
   重新打包后发布 v1.1 才能收到数据。

> 不部署云函数也能分发（telemetry 默认不联网），只是收不到玩家数据。

## 四、日常看数据

云函数有数据后：`server_cloud/analyze.py` 本地跑（需 `pip install cos-python-sdk-v5`）输出
西瓜率 / 按策略对比 / 断链等级分布。

## 版本节奏建议

- 收集到 ≥50 局真实数据前，只做小 bug 修复，别动策略权重（样本不足无法判断）。
- ≥100 局后做「断链等级」分析（哪个等级之后合成最常中断），再针对性改 solver。
- 策略改动后发新 release，让玩家帮忙 A/B。
